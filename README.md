Simple docker registry that auto-imports the images you put in a names file.

Mount your images folder into /images/names like this:

<file>      <tag>   <version>
gluster.tar gluster latest

See Makefile to see how to build and run it.
